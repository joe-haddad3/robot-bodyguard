# Robot Bodyguard — project dependencies
# Install with: pip install -r requirements_project.txt
#
# On Raspberry Pi 5, install OpenCV headless (no GUI dependencies):
#   pip install opencv-python-headless
# On PC (with display):
#   pip install opencv-python

# Core vision
torch>=2.0.0
torchvision>=0.15.0
opencv-python>=4.8.0        # change to opencv-python-headless on RPi if needed
numpy>=1.24.0
Pillow>=9.0.0

# Face recognition
facenet-pytorch>=2.5.3

# Pose / face / hand landmark detection
mediapipe>=0.10.0

# YOLOv5 dependencies
PyYAML>=6.0
requests>=2.28.0
scipy>=1.10.0
tqdm>=4.64.0
matplotlib>=3.7.0
seaborn>=0.12.0
pandas>=1.5.0
psutil>=5.9.0
thop>=0.1.1

# Tracking
ultralytics>=8.4.14
lap>=0.5.13
